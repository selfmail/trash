import { Text, View } from "@/src/components/Themed";

export default function CreateAddressScreen() {
	return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text>Create Address Screen</Text>
        </View>
    );
}